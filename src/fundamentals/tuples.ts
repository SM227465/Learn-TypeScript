/* TypeScript has a new type call Tuple which is a fixed length array where each element has a particular type, we often use them when working with pair of values
for example each user we wanna represent two values ID and Name 
Recomended for 2 values, NOT Recomended if there is more than 2 values */

let user: [number, string] = [1, 'Raizo'];
