// for number type => array should contain only numbers
let numbers: number[] = [1, 2, 3];

// string type
let strings: string[] = ['a', 'b', 'c'];

// object type
let objects: object[] = [{ name: 'Raizo', age: 25 }];
